# 🐳 Docker-Develop-Server-Setup

این اسکریپت Bash به‌صورت خودکار یک سرور توسعه ضروری مبتنی بر Docker را روی سیستم‌عامل‌های مبتنی بر Debian/Ubuntu راه‌اندازی می‌کند. این تنظیمات شامل نصب و به‌روزرسانی سیستم، داکر و استقرار ابزارهای کلیدی مدیریت Docker و پروکسی است.

## 🚀 عملکرد اصلی

* **به‌روزرسانی و ارتقاء سیستم:** اجرای کامل `apt update` و **`apt upgrade -y`**.
* **نصب ابزارهای عمومی:** نصب ابزارهای ضروری مانند `tmux`، `nano`، `git` و `curl`.
* **نصب داکر و داکر کامپوز:** نصب Docker Engine و Docker Compose Plugin (توصیه شده).
* **شبکه اختصاصی:** ایجاد شبکه Docker داخلی به نام **`kitzone-net`**.
* **استقرار سرویس‌ها:** نصب Nginx Proxy Manager (NPM) و Portainer CE.

## 📦 سرویس‌های نصب شده

| سرویس | توضیحات | پورت دسترسی |
| :--- | :--- | :--- |
| **Docker Engine & Compose** | پلتفرم اصلی کانتینرسازی و مدیریت توسط داکر. | N/A |
| **Nginx Proxy Manager (NPM)** | پروکسی معکوس قدرتمند با پنل مدیریتی برای مدیریت آسان SSL/TLS. | **80, 443, 81** (پنل مدیریت) |
| **Portainer CE** | رابط کاربری گرافیکی برای مدیریت آسان محیط داکر و کانتینرها. | **9000** |

## 🛠️ پیش‌نیازها

* **سیستم‌عامل:** Debian یا Ubuntu (و مشتقات آن‌ها).
* **دسترسی:** نیاز به اجرای اسکریپت با کاربر **`root`** یا استفاده از **`sudo`**.

## ⚡ دستور نصب تک‌خطی (One-Liner)

برای اجرای کامل اسکریپت و نصب بدون نیاز به دانلود دستی فایل، دستور زیر را اجرا کنید. این دستور فایل `setup.sh` را مستقیماً از مخزن شما دانلود و با دسترسی کامل اجرا می‌کند:

> **هشدار امنیتی:** همیشه قبل از اجرای اسکریپت‌ها به‌صورت مستقیم از اینترنت، محتوای آن را بررسی کنید.

```bash
sudo -E bash -c "$(curl -fsSL https://raw.githubusercontent.com/BigPyth0n/Docker-Develop-Server-Setup/main/setup.sh)"
